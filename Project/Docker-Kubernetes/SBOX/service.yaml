apiVersion: v1
kind: Service
metadata:
 name: webapp-application-service-sbox
spec:
 ports:
 - name: http
   protocol: TCP
   port: 80
   targetPort: 8080
 selector:
   app: my-app-sbox
 type: NodePort
